import styled from "styled-components";

const pageTitle = styled.div`
  width: 90%;

  h2 {
    font-size: 24px;
  }

  /* margin: 10px;

  :hover {
    color: red;
  } */
`;

export default { pageTitle };
