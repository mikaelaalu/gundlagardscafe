import styled from "styled-components";

const imageWrapper = styled.div`
  width: 100vw;
  height: 100vw;

  img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
`;

export default { imageWrapper };
