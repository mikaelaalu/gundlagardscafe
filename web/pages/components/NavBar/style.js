import styled from "styled-components";

const nav = styled.nav`
  width: 100%;
  height: 102px;
  border-bottom: 1px solid black;
  padding: 0 20px;
  display: flex;
  justify-content: space-between;
`;

const logo = styled.div`
  padding: 15px 0;
`;

export default { nav, logo };
